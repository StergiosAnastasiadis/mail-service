import asyncHandler from 'express-async-handler'

const activateAccount = asyncHandler(async (req, res) => {
    // const { email, firstname, lastname, activationToken, isActivated } = req.body


    // res.send('email here')
})

export { activateAccount }